# Sincronização upstream — 2026-09-08

## Referências

- fork antes do merge: `0d499103`;
- upstream integrado: `cee557d46d63e3407dc3bbda6480c0c674e8bb80`;
- ponto comum: `c2e807f8a3ff90eaa4b6867e038c277226466c4e`;
- upstream: 28 commits desde o ponto comum.

## Resultado

O Git realizou merge automático de todos os caminhos sobrepostos, exceto
`src/Cafe/OS/libs/swkbd/swkbd.cpp`.

### Resolução do teclado virtual

Foram preservados do upstream:

- exports tipados com `cafeExportRegisterFunc`;
- inicialização e cálculo de subthread;
- estrutura e inicialização do learn dictionary;
- correções de endian e assinaturas das funções.

Foram preservados do port Android:

- `swkbdCallbacks`;
- abertura do teclado Android em `SwkbdAppearInputForm` e
  `SwkbdAppearKeyboard`;
- fechamento do teclado Android nas duas rotas de desaparecimento;
- API namespaced usada pelo JNI e pelos frontends.

## Mudanças upstream relevantes incorporadas

- restauração do tamanho real do bloco após processar jumps no backend AArch64;
- tratamento fatal de erros inesperados de fence Vulkan;
- `vertexPipelineStoresAndAtomics` condicionado ao suporte do dispositivo;
- correção do cache de estado de erro de shader;
- correções de Latte, H264, RPL, PPC assembler e serviços HLE;
- atualizações do overlay port e versão do wxWidgets para desktop.

## Validação

- nenhum marcador de conflito restante;
- `git diff --check` limpo;
- referências antigas `swkbd_render`, `swkbd_keyInput` e
  `swkbd_hasKeyboardInputHook` não permanecem em consumidores;
- submódulos continuam nos commits fixados pelo port.

A compilação local continua bloqueada pela ausência de SDK/NDK e pelo acesso
indisponível ao host de distribuição do Gradle. O merge não deve entrar em uma
release até passar pelo workflow Android e por smoke test em dispositivo.

